import React from "react";

export default function K8sInBits() {
    return (
        <article className="prose prose-invert max-w-none">
            <h1>K8s in bits</h1>
            <p>Archive of links and notes about Kubernetes — add your content here using plain JSX.</p>
            {/* write full post UI here using standard React/JSX; keep it simple */}
        </article>
    );
}